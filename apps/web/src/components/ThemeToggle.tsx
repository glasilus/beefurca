"use client";

import React from "react";

/**
 * Переключение тем убрано в упрощённой версии (единая тёмная PC-98 палитра).
 * Компонент сохранён как no-op, чтобы не править все места его использования.
 */
export const ThemeToggle: React.FC<{ className?: string }> = () => null;
